export type FlashData = {
  notice?: string
  alert?: string
}

// Extend with props shared from InertiaController via inertia_share
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type SharedProps = {}
